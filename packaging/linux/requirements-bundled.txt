# Python dependencies installed into /usr/lib/solokeys-gui/site-packages
# by the package postinst script. Versions kept in sync with pyproject.toml.
# PySide6 and solo2 are excluded: PySide6 is a system dep; solo2 is shipped
# directly from source in the package.
fido2>=1.1.2
requests>=2.31.0
pyusb>=1.2.1
qtawesome>=1.3.0
pyscard>=2.0.9
hidapi>=0.14.0.post2
